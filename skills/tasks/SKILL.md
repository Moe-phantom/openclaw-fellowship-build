---
name: tasks
description: "List all open tasks from tasks.jsonl. Usage: /tasks"
user-invocable: true
---

# tasks

Use the exec tool to run:

```
/usr/bin/bash /home/mohamed-alwathiq/.openclaw/workspace/skills/tasks/scripts/tasks.sh
```

Reply with the stdout output of that command, verbatim. Nothing else.
